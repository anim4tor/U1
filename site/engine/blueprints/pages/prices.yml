title: Prices

status:
  draft: true
  unlisted: true
  listed: true

tabs:
  content:
    fields:
      hero:
        extends: fields/hero

      single:
        type: structure
        batch: true
        fields:
          label:
            type: text
          duration:
            type: select
            options:
              - 60
              - 75
              - 90
            after: min
          price:
            type: text
            after: Kč


      passes:
        type: structure
        batch: true
        fields:
          label:
            type: text
          duration:
            type: select
            options:
              - 60
              - 75
              - 90
            after: min
          price:
            type: text
            after: Kč

      credit:
        extends: fields/header
        label: Credit

      membership:
        type: structure
        batch: true
        fields:
          label:
            type: text
          price:
            type: text
            after: Kč

      benefits:
        extends: fields/header
        label: Benefits

  meta: tabs/meta/page